# GitHub Personal Access Token (optional but recommended)
# Without this token, you'll be limited to 60 requests per hour
# With this token, you get 5,000 requests per hour
# 
# To create a token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token" -> "Generate new token (classic)"
# 3. Give it a name like "GitHub Profile Explorer"
# 4. Select scopes: "public_repo" and "read:user" (or just leave default)
# 5. Click "Generate token"
# 6. Copy the token and paste it below
#
REACT_APP_GITHUB_TOKEN=ghp_Ttd2MFiCdxuzvf1kw0mgwAhEcTQQQU0u7UuG

# Optional: Set to 'true' to enable debug logging
REACT_APP_DEBUG_API=false